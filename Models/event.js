class Event{
    constructor(id, title, info, date, image)
    {
        this.id=id
        this.title=title
        this.info=info
        this.date=date
        this.image=image
    }
}
export default Event;